1) this
1.1 Создать 10 элементов div с классом apple.
Прослушать событие клик всех элементов.
При клике на элемент обращаться от текущего элемента к выше стоящиму и окрашивать его фон в зеленый цвет.
Дополнительно:
1.2 Выделять только один элемент. Чтобы при клике на следуюшие элементы цвета добавленные ранее убирались.
1.3 Сделать случайный выбор. Либо выделяем выше стоящий либо ниже.
1.4 Сделать возможность случайного цвета при клике.

2) call, bind
2.1 call
Создать функцию, которая копирует объект и возвращает его.
Функция не принимает аргументов. Работа с объектом внутри функции через ключевое слово this. Функция перебирает объект и возвращает новый объект.

2.2 bind
Функция, которая принимает случайный аргумент и через таймер бесконечно выводит.
Создать массив fruits со значениями: apple, orange, plum
Создать функцию, которая принимает аргумент и выводит его через консоль
Создать таймер, который принимает функцию по ссылке. Функция принимает в качестве аргумента значение из массива. А вместо индекса используется Math.random для генерации случайного числа
На выходе в консоле должно выводитс много раз какое-то одно из значений массива. 